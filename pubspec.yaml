name: flutter_a_c_soluciones
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.19.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  provider: ^6.0.0
  http: ^1.5.0
  bloc: ^9.1.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.5
  jwt_decoder: ^2.0.1
  flutter_secure_storage: ^9.2.4
  url_launcher: ^6.3.2
  intl: ^0.20.2
  path_provider: ^2.1.5
  permission_handler: ^12.0.1
  open_file: ^3.5.10
  fl_chart: ^0.66.2
  image_picker: ^1.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^10.0.0
  mockito: ^5.4.4
  mocktail: ^1.0.0
  flutter_launcher_icons: ^0.13.1
  build_runner: ^2.4.11
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true

  assets:
    - assets/

flutter_icons:
  android: true
  ios: true
  image_path: "assets/soluciones.png"
